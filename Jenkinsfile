@Library('jenkins-shared-library') _

def configMap = [
    Project : "roboshop"
    component : "catalogue"
]
samplePipeline(configMap) // by default it will call, call function inside this pipelilne